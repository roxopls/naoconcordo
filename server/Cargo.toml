[package]
name = "naoconcordo-server"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = { version = "0.8", features = ["ws"] }
base64 = "0.22"
chrono = { version = "0.4", features = ["serde"] }
hmac = "0.12"
jsonwebtoken = "9"
pbkdf2 = "0.12"
rand = "0.9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
tokio = { version = "1", features = ["full"] }
# `fs` serve os arquivos de atualizacao para quem hospeda no Windows, onde nao
# ha Caddy na frente para fazer isso.
tower-http = { version = "0.6", features = ["cors", "limit", "trace", "fs"] }
# Busca de GIF: o servidor fala com o Tenor para o cliente nao ter de falar.
# `rustls` em vez do TLS do sistema para o binario de Windows nao depender
# de nada instalado na maquina de quem hospeda.
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls", "charset", "http2"] }
uuid = { version = "1", features = ["v4", "serde"] }

